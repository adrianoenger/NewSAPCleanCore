# Controlled Backlog

This file captures useful discoveries that are outside the active sprint. Recording an item here does **not** authorize implementation.

| ID | Title | Description | Origin | Priority | Suggested Sprint | Status |
|---|---|---|---|---|---|---|
| BL-001 | Remove duplicate root `gitignore` file | Repository root contains `gitignore` (no dot), identical to `.gitignore` and without effect. Remove it or confirm it is intentional. | SPRINT-00 hygiene review | Low | Any (administrative) | Open |
| BL-002 | Wrap ATC import as an optional pipeline stage | SPRINT-06's durable pipeline wraps scan/parse/detect_dependencies; ATC import stays a standalone upload-triggered action (ADR-016) since it requires a user-supplied file per run and has no natural work-item decomposition today. Could become a 4th DAG stage once a "re-run last uploaded ATC file" use case exists. | SPRINT-06 design | Low | Future | Open |
| BL-003 | Wrap future AI-processing stages (SPRINT-09+) in the same PipelineRun/StageRun/WorkItem engine | The engine in `backend/src/pipeline/` is generic (stage handlers registered via `StageDefinition`); object understanding, business-rule discovery, etc. should register as additional stages instead of building a parallel execution mechanism. | SPRINT-06 design | Medium | SPRINT-09+ | Open |
| BL-004 | Stabilize SAPObject identity/correlations across reprocessing | `ATCFinding.correlated_object_id` and new supplemental evidence correlations cannot safely depend on SAPObject rows that are deleted/recreated during parse. SPRINT-08 must establish stable identity/upsert semantics or deterministic re-correlation before downstream evidence use. | SPRINT-07 design + ADR-017 | High | SPRINT-08 | Resolved (SPRINT-08: `sap_object.canonical_key`/`last_seen_stage_run_id`, upsert in `pipeline/stages.py::_parse_process_item`, reconciliation in `_parse_finalize`, migration 0008 dedup + FK repoint) |
| BL-005 | Normalize SAP short type codes for evidence/ATC correlation exact-match | External exports (Panaya, and potentially ATC) use SAP short type codes (`CLAS`, `FUGR`, `PROG`, `TABL`, ...) while internal `SAPObject.object_type` uses long-form strings (`class`, `function_module`, `report`, `ddic_table`). `evidence.correlation.correlate_dataset_records` and ATC's `_correlate_findings` both compare types verbatim (case-insensitive only), so a genuinely exact match degrades to `MATCHED_HEURISTIC` whenever the source uses SAP short codes — confirmed live in SPRINT-08's manual browser validation (Panaya `type="CLAS"` vs internal `class`). A shared type-alias table would let both correlators upgrade these to `MATCHED_EXACT`. | SPRINT-08 live validation | Low | Future | Open |
| BL-006 | Panaya adapter batch re-scan cost at production scale | Superseded — the adapter was rewritten against a real ~2.8GB export (see ADR-017 amendment). Batching is now one curated section per batch (5 batches, not thousands), each re-streamed from the top and stopped as soon as its section closes; validated end-to-end against the real file in ~230s. Re-streaming per batch is still O(sections × partial-file-length) but is bounded and acceptable at this granularity — no longer the concern this item originally described. | SPRINT-08 design (original) → resolved by the Panaya rewrite | Low | — | Resolved |
| BL-007 | Consider a dedicated test database instead of scoped-cleanup discipline | Every `backend/tests/*.py::_cleanup` used to run unscoped `DELETE FROM {tbl}` against the shared dev Postgres, twice destroying real user data during SPRINT-08 (see `docs/delivery/SESSION_HANDOFF.md` and the `project-test-isolation` memory). Fixed by scoping every cleanup to the specific client/assessment id each test created (cascades via existing FKs) — but this depends on every future test file following the same discipline. A `CCA_TEST_DATABASE_URL` pointing at an isolated database would remove the risk architecturally instead of relying on convention. | SPRINT-08 incident (user-reported "processing forever" bug investigation) | Medium | Future | Open |
| BL-008 | `pipeline.engine.recover_orphans` is global, not scoped, when called from tests | `recover_orphans(session)` resets *every* `running` `PipelineRun`/`StageRun`/`WorkItem` in the database to `pending`/`paused` — correct behavior for its real use (app-startup crash recovery), but `test_sprint06.py::test_recover_orphans_requeues_running_work` calls it directly against the shared dev database; a real in-flight pipeline run at that exact moment would be incorrectly paused. Lower severity than BL-007 (single test, not the whole suite) and not fixed in SPRINT-08. **Reproduced live in SPRINT-13**: running the full backend pytest suite concurrently with a live Playwright pipeline demo (a) paused the demo's own in-flight run mid-execution, and (b) made the test itself see 2 orphans instead of the 1 it manufactured. **Reproduced again in SPRINT-17**: an unrelated stray `PipelineRun` (assessment `Assessment01`/Rodobens, stuck `running` on `object_understanding` from an earlier, unrelated session against the shared dev database) made `test_recover_orphans_requeues_running_work` fail with `assert 2 == 1`; resolved by calling `recover_orphans` once directly (the same crash-recovery action the app performs at startup) to clear the stray row, after which the full suite passed cleanly. Both transient — no code change; still open. | SPRINT-08 incident follow-up; reproduced SPRINT-13 and SPRINT-17 live validation | Low | Future | Open |
| BL-009 | Extend Panaya's curated section list | `evidence/adapters/panaya.py::_CURATED_SECTIONS` maps only 5 of the ~90 real top-level sections (`REPOSITORY_OBJECTS`, `PROGRAMS`, `FUNCTIONS`, `MODIFICATIONS`, `NOTES_HEADER`) to `EvidenceRecord`s — chosen because their attribute shape was concretely verified against a real export. `WHERE_USED_TABLE`/`WHERE_USED_METHOD_MAP` (dependency/where-used, ADR-017's stated priority) and `SCI_HANA_ISSUES(_DETAILS)`/`SI_CHECKS` (S/4 conversion signals) are known-valuable but not yet mapped — every section is still counted in `inspect()`'s manifest either way, so nothing is invisible, just not yet extracted as records. | SPRINT-08 real-file validation | Medium | Future | Open |
| BL-010 | `correlate_dataset_records` loads every EvidenceRecord of a dataset as ORM objects in one pass | Resolved — a real Signavio import (4.4M records for one dataset) pushed a naive all-at-once pass to ~20GB RSS, confirming the concern this item raised speculatively. Fixed with id-ordered pagination (5000 records/page, `evidence/correlation.py::_PAGE_SIZE`) plus executemany-style `insert()` for the correlation rows and per-page `session.expunge()` (never `expunge_all()`, which would silently detach caller-owned objects like the evidence-import `dataset`). Full real file now correlates with memory staying under ~350MB. | SPRINT-08 real-file validation (originally speculative, later confirmed) | — | — | Resolved |
| BL-011 | Panaya/HANA-sizing/Readiness-Check adapters still use a per-row SELECT-then-INSERT existence check | Signavio was rewritten to a chunked bulk `INSERT ... ON CONFLICT DO NOTHING` (see `evidence/adapters/base.py::bulk_insert_evidence_records`) after a real ~4.4M-row import proved the per-row pattern doesn't scale. The other three adapters still check-then-insert one row at a time, but at far smaller confirmed real-world scale (Panaya's curated sections cap at 20k rows each; HANA sizing/Readiness Check are in the hundreds) — not yet proven to need the same fix. Apply `bulk_insert_evidence_records` to them if/when a real file proves it necessary, rather than pre-emptively. | SPRINT-08 real-file validation (Signavio) | Low | Future (only if proven necessary) | Open |
| BL-012 | No automatic deduplication of re-imported evidence packages | Each upload of `POST /evidence-datasets` always creates a new independent `EvidenceDataset` row, even when `source_sha256` exactly matches an existing dataset, or when a different file of the same provider/type is uploaded again (e.g. a second Panaya export). This is partly intentional (ADR-017: "re-importing a new version must not silently overwrite historical provenance"), but an accidental true duplicate double-counts `records_count` and correlations with no warning. The dataset-delete endpoint (`DELETE /evidence-datasets/{id}`, SPRINT-08 post-completion) gives the user a manual way to remove an accidental duplicate; a same-sha256 warning at upload time would close the gap proactively. | User question during SPRINT-08 manual testing | Low | Future | Open |
| BL-013 | Pre-existing schema/ORM-model drift on `sap_object`/`technical_finding` | `alembic revision --autogenerate` for SPRINT-09's `object_understanding` table also proposed dropping/recreating `sap_object`'s `ux_sap_object_assessment_canonical_key` (a plain unique index today vs. a named `UniqueConstraint` in the model) and adding `ix_sap_object_canonical_key`/`ix_technical_finding_atc_finding_id` (declared `index=True` in `persistence/models.py` but never migrated). Harmless — both forms enforce the same constraint/lookup — but autogenerate will keep re-proposing this diff on every future migration until a dedicated migration reconciles it. Left untouched in SPRINT-09's migration (out of scope). | SPRINT-09 `alembic revision --autogenerate` | Low | Future | Open |
| BL-014 | Pre-existing SPRINT-06/07/08 tests now transitively make live Bedrock calls | Per BL-003, `object_understanding` registers as a 4th `source_processing` stage (`pipeline/stages.py::STAGES_BY_KIND`) — this is the intended architecture, not a defect. But every pre-existing test that runs the full pipeline via `run_pipeline`/`POST .../pipeline-runs` without mocking `pipeline.stages.get_provider` (most of `test_sprint06/07/08.py`) now makes a real Bedrock Converse API call per SAP object, since this dev environment has real AWS credentials mounted. Confirmed harmless today (all such tests still pass), but it triples the full suite's runtime (~7s → ~134s) and makes unrelated scan/parse/dependency-detection/evidence tests fail if Bedrock becomes unreachable or credentials are ever removed. SPRINT-09's own new tests (`test_ai_providers.py`, `test_object_understanding_pipeline.py`) mock the provider throughout; retrofitting the same mock into the older sprint files was judged out of scope for this sprint (no new capability, and those tests still pass for real). | SPRINT-09 `object_understanding` stage integration (CAP-009) | Low | Future | Open |
| BL-015 | Stray un-cleaned-up dev-database clients from prior live-validation sessions | The Assessments Home list shows a "SPRINT09 Live Smoke Test" client alongside the real Acme/Rodobens demo data — a prior session's live Bedrock/Playwright validation apparently was not fully cleaned up despite its own result record claiming it was. Not a data-loss risk (test/demo data only, not customer data), but it clutters the client picker. SPRINT-10's own two disposable clients (`Sprint10 Live Smoke`, `Sprint10 UI Smoke`) were deleted via scoped `DELETE FROM client WHERE id=:cid` immediately after use. | SPRINT-10 live/UI smoke validation | Low | Future (manual cleanup or a documented dev-db reset step) | Open |
| BL-016 | Application clustering does not yet use process/usage correlation as a grouping signal | `ai/application_discovery/clustering.py` clusters on resolved dependency edges, shared `package_name`(`_raw`) and shared `ObjectUnderstanding.concepts` — it deliberately does not attempt a "process/usage correlation" signal (Baseline: "packages/transactions/semantic signals plus reliable process/usage correlations when available") because `EvidenceRecord`/`EvidenceCorrelation` has no generic, provider-agnostic cross-object join key for that beyond `package_name` (already covered); adding one would mean guessing a provider-specific payload shape, which ADR-017 forbids. Revisit once a supplemental-evidence adapter exposes a concrete, documented process/usage correlation key. | SPRINT-11 design (CAP-001) | Low | Future | Open |
| BL-017 | ATC finding detail has no "SAP Guidance" block | SPRINT-12's `SapGuidancePanel` was wired into `ApplicationBrowser`/`BusinessRuleBrowser` (both already have a selectable-list + detail-panel layout). `ATCImport.tsx`'s findings are a flat table inside an expandable run row with no per-finding detail view/route, so adding "SAP Guidance" there would require a new detail affordance, not just plugging in the existing panel — judged out of scope for this sprint's minimal-checkpoint goal. `sap_knowledge` target-kind routing (`api/routes/sap_knowledge.py::_TARGET_KIND_TO_TYPE`) is structured so an `"atc-findings"` kind can be added later without changing the persisted-evidence model. | SPRINT-12 design | Low | Future | Open |
| BL-018 | SAP Knowledge MCP adapter (`ai/knowledge_providers/mcp_client.py`) is unit-tested only, no real `mcp-sap-docs`/`mcp-abap` server available | No Docker image/endpoint for either MCP server exists in this environment (unlike Bedrock, which had real credentials). The adapter targets the real `mcp` Python SDK's streamable-HTTP transport (`mcp==2.2.0`, confirmed against the installed package during SPRINT-12) and is unit-tested against a mocked transport (`tests/test_sap_knowledge.py`) plus a live-UI smoke check confirming the "no providers configured" empty-state path end to end — mirrors how Azure AI Foundry shipped unit-tested-only in SPRINT-09. Live retrieval end-to-end (a real `search` tool call against a running `mcp-sap-docs`/`mcp-abap` container) is deferred until such a server/endpoint is actually available. | SPRINT-12 implementation | Medium | Future (when a real MCP server/endpoint is available) | Open |
| BL-019 | No manual user-override/validation for the Clean Core recommendation | `CleanCoreAssessment` (SPRINT-13) has no PATCH/validation endpoint analogous to `BusinessRule.user_validated` or `Application`'s rename/merge actions — the AI-produced technical_risk/business_importance/recommendation cannot be corrected by an analyst, only regenerated by reprocessing. Not in SPRINT-13's planned capabilities; worth reconsidering alongside SPRINT-15's Functional View user-validation work if a demo needs it. | SPRINT-13 design | Low | Future (SPRINT-15 or later, if requested) | Open |
| BL-020 | Reprocessing leaves orphaned `Embedding` rows for entities `business_rule_discovery` recreates | `business_rule_discovery`'s `_business_rule_discovery_process_item` deletes and reinserts every non-user-validated `BusinessRule` row per reprocessing run (fresh `id` each time) rather than upserting in place, unlike `ObjectUnderstanding`/`Application`/`CleanCoreAssessment`. The `embeddings` stage (SPRINT-14) keys `Embedding.entity_id` off that same `id`, so a reprocessed rule's old `Embedding` row is never cleaned up — it becomes a dangling, unreachable-by-join row that still counts toward `Embedding` table size and could theoretically surface in a semantic search result for a rule that no longer exists. Low severity for the PoC's demo scale; a real fix would need either an `embeddings` stage `finalize` that prunes `Embedding` rows whose `entity_id` no longer resolves, or `business_rule_discovery` switching to upsert-by-a-stable-key like the other AI stages. | SPRINT-14 pipeline-integration test (`test_embeddings_incrementality_skips_unchanged_content`) | Low | Future | Open |

| BL-021 | Baseline's "customisation identification" AI step has no dedicated capability | The Baseline's canonical AI Processing flow lists "customisation identification" as a distinct step from "application discovery", but no sprint has implemented it as its own AI capability — `application_discovery` (SPRINT-11) is the closest existing signal (a cluster of custom SAP objects). SPRINT-15's Dashboard Geral/Preliminary Processing Summary "Customizações identificadas" KPI maps to the count of discovered `Application` rows rather than a distinct AI judgment, to avoid fabricating a new capability outside this sprint's navigation/perspectives scope. | SPRINT-15 design (dashboard-summary endpoint) | Low | Future | Open |
| BL-022 | No application-to-application coupling/dependency graph for Architecture View | Baseline lists "coupling patterns" as Architecture View content. `SAPObjectDependency` (SPRINT-05) stores `target_name`/`target_type` as free text, never resolved to a target `SAPObject.id` — there is no existing mechanism to tell whether a dependency edge crosses an `Application` boundary (mirrors BL-016's same root gap for clustering signals). SPRINT-15 ships the achievable part of Architecture View drill-down (clickable business-rule/finding lists) without inventing a new dependency-resolution mechanism; a real coupling view needs `SAPObjectDependency` target resolution first. | SPRINT-15 design (CAP-006 scope narrowing) | Low | Future | Open |
| BL-023 | Copilot reference chips for finding/dataset/guidance citations are not click-through | `CopilotPanel.tsx::ReferenceChips` renders every `evidence_refs` citation (including `TECHNICAL_FINDING`, `SUPPLEMENTAL_EVIDENCE`, `SAP_KNOWLEDGE` source types) as an inert label with a tooltip only. Only the single AI-chosen `navigation_ref` is clickable, and only when it resolves to one of the three kinds `resultNav.ts::ResultFocus` supports today (`sap_object`/`business_rule`/`application`) — there is no navigable destination for an ATC finding, a supplemental-evidence dataset/record, or a SAP Knowledge reference. Extending `ResultFocus` to those kinds (and giving each a detail affordance to land on) was judged a bigger architecture change than SPRINT-16's planned "return answer references and navigation actions" capability, which this sprint satisfies for the entity kinds that already have one. | SPRINT-16 design (CAP-004/CAP-007 scope narrowing) | Low | Future | Open |
| BL-024 | Monaco's editor web worker cannot be wired up under electron-vite's Rollup build | `SourceViewer.tsx` (SPRINT-17 CAP-002) runs Monaco without a `MonacoEnvironment.getWorker` — both the documented `?worker` suffix import and `new URL(..., import.meta.url)` fail to resolve `monaco-editor/esm/vs/editor/editor.worker.js` through electron-vite's Rollup build (tried both live; the former errors at build time with "Rollup failed to resolve import", the latter with "Could not resolve entry module"). Monaco falls back to running its editor services on the main thread, which is functionally fine for this read-only viewer (no lint/format language services used) but logs one console error per opened file. Revisit with a dedicated Vite/Monaco bundling plugin (e.g. `vite-plugin-monaco-editor-esm`) if a future sprint needs worker-backed language features (autocomplete, live diagnostics) instead of just display. | SPRINT-17 live Playwright validation | Low | Future (only if worker-backed language features are needed) | Open |
| BL-025 | Pre-existing `scan.source_path` + `rel_path` joins elsewhere have no containment check | SPRINT-17's automated security review flagged (and this sprint fixed) a path-traversal gap in the new `GET .../objects/{id}/source` endpoint — `os.path.join(scan.source_path, source_file.rel_path)` with no verification the result stays under `scan.source_path`. The same join pattern, without containment checking, pre-exists in `api/routes/dependencies.py` (`detect_and_persist_dependencies`/`detect_all_dependencies`) and `pipeline/stages.py` (`_dependencies_process_item`) — lower severity there since none of them return raw file content over HTTP (only feed it to the internal dependency detector), but a poisoned/legacy `rel_path` could still cause them to read an arbitrary file on the container filesystem. Apply the same `realpath`+`commonpath` containment check (and reject absolute `rel_path`) if any of these ever start returning content, or proactively as defense in depth. | SPRINT-17 automated security review (CAP-002) | Low | Future | Open |

## Rules
- Use a stable ID such as `BL-001`.
- Do not implement backlog items during the active sprint unless they are promoted into canonical sprint scope.
- A prerequisite defect that blocks the sprint is not backlog; it may be fixed as part of the active sprint and documented in progress.
- Architectural proposals must also respect ADR governance; a backlog entry cannot override an accepted ADR.
