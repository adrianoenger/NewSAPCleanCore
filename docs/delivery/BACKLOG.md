# Controlled Backlog

This file captures useful discoveries that are outside the active sprint. Recording an item here does **not** authorize implementation.

| ID | Title | Description | Origin | Priority | Suggested Sprint | Status |
|---|---|---|---|---|---|---|
| BL-001 | Remove duplicate root `gitignore` file | Repository root contains `gitignore` (no dot), identical to `.gitignore` and without effect. Remove it or confirm it is intentional. | SPRINT-00 hygiene review | Low | Any (administrative) | Open |
| BL-002 | Wrap ATC import as an optional pipeline stage | SPRINT-06's durable pipeline wraps scan/parse/detect_dependencies; ATC import stays a standalone upload-triggered action (ADR-016) since it requires a user-supplied file per run and has no natural work-item decomposition today. Could become a 4th DAG stage once a "re-run last uploaded ATC file" use case exists. | SPRINT-06 design | Low | Future | Open |
| BL-003 | Wrap future AI-processing stages (SPRINT-09+) in the same PipelineRun/StageRun/WorkItem engine | The engine in `backend/src/pipeline/` is generic (stage handlers registered via `StageDefinition`); object understanding, business-rule discovery, etc. should register as additional stages instead of building a parallel execution mechanism. | SPRINT-06 design | Medium | SPRINT-09+ | Open |
| BL-004 | Stabilize SAPObject identity/correlations across reprocessing | `ATCFinding.correlated_object_id` and new supplemental evidence correlations cannot safely depend on SAPObject rows that are deleted/recreated during parse. SPRINT-08 must establish stable identity/upsert semantics or deterministic re-correlation before downstream evidence use. | SPRINT-07 design + ADR-017 | High | SPRINT-08 | Resolved (SPRINT-08: `sap_object.canonical_key`/`last_seen_stage_run_id`, upsert in `pipeline/stages.py::_parse_process_item`, reconciliation in `_parse_finalize`, migration 0008 dedup + FK repoint) |
| BL-005 | Normalize SAP short type codes for evidence/ATC correlation exact-match | External exports (Panaya, and potentially ATC) use SAP short type codes (`CLAS`, `FUGR`, `PROG`, `TABL`, ...) while internal `SAPObject.object_type` uses long-form strings (`class`, `function_module`, `report`, `ddic_table`). `evidence.correlation.correlate_dataset_records` and ATC's `_correlate_findings` both compare types verbatim (case-insensitive only), so a genuinely exact match degrades to `MATCHED_HEURISTIC` whenever the source uses SAP short codes — confirmed live in SPRINT-08's manual browser validation (Panaya `type="CLAS"` vs internal `class`). A shared type-alias table would let both correlators upgrade these to `MATCHED_EXACT`. | SPRINT-08 live validation | Low | Future | Open |
| BL-006 | Panaya adapter batch re-scan cost at production scale | Superseded — the adapter was rewritten against a real ~2.8GB export (see ADR-017 amendment). Batching is now one curated section per batch (5 batches, not thousands), each re-streamed from the top and stopped as soon as its section closes; validated end-to-end against the real file in ~230s. Re-streaming per batch is still O(sections × partial-file-length) but is bounded and acceptable at this granularity — no longer the concern this item originally described. | SPRINT-08 design (original) → resolved by the Panaya rewrite | Low | — | Resolved |
| BL-007 | Consider a dedicated test database instead of scoped-cleanup discipline | Every `backend/tests/*.py::_cleanup` used to run unscoped `DELETE FROM {tbl}` against the shared dev Postgres, twice destroying real user data during SPRINT-08 (see `docs/delivery/SESSION_HANDOFF.md` and the `project-test-isolation` memory). Fixed by scoping every cleanup to the specific client/assessment id each test created (cascades via existing FKs) — but this depends on every future test file following the same discipline. A `CCA_TEST_DATABASE_URL` pointing at an isolated database would remove the risk architecturally instead of relying on convention. | SPRINT-08 incident (user-reported "processing forever" bug investigation) | Medium | Future | Open |
| BL-008 | `pipeline.engine.recover_orphans` is global, not scoped, when called from tests | `recover_orphans(session)` resets *every* `running` `PipelineRun`/`StageRun`/`WorkItem` in the database to `pending`/`paused` — correct behavior for its real use (app-startup crash recovery), but `test_sprint06.py::test_recover_orphans_requeues_running_work` calls it directly against the shared dev database; a real in-flight pipeline run at that exact moment would be incorrectly paused. Lower severity than BL-007 (single test, not the whole suite) and not fixed in SPRINT-08. | SPRINT-08 incident follow-up | Low | Future | Open |
| BL-009 | Extend Panaya's curated section list | `evidence/adapters/panaya.py::_CURATED_SECTIONS` maps only 5 of the ~90 real top-level sections (`REPOSITORY_OBJECTS`, `PROGRAMS`, `FUNCTIONS`, `MODIFICATIONS`, `NOTES_HEADER`) to `EvidenceRecord`s — chosen because their attribute shape was concretely verified against a real export. `WHERE_USED_TABLE`/`WHERE_USED_METHOD_MAP` (dependency/where-used, ADR-017's stated priority) and `SCI_HANA_ISSUES(_DETAILS)`/`SI_CHECKS` (S/4 conversion signals) are known-valuable but not yet mapped — every section is still counted in `inspect()`'s manifest either way, so nothing is invisible, just not yet extracted as records. | SPRINT-08 real-file validation | Medium | Future | Open |
| BL-010 | `correlate_dataset_records` loads every EvidenceRecord of a dataset as ORM objects in one pass | Resolved — a real Signavio import (4.4M records for one dataset) pushed a naive all-at-once pass to ~20GB RSS, confirming the concern this item raised speculatively. Fixed with id-ordered pagination (5000 records/page, `evidence/correlation.py::_PAGE_SIZE`) plus executemany-style `insert()` for the correlation rows and per-page `session.expunge()` (never `expunge_all()`, which would silently detach caller-owned objects like the evidence-import `dataset`). Full real file now correlates with memory staying under ~350MB. | SPRINT-08 real-file validation (originally speculative, later confirmed) | — | — | Resolved |
| BL-011 | Panaya/HANA-sizing/Readiness-Check adapters still use a per-row SELECT-then-INSERT existence check | Signavio was rewritten to a chunked bulk `INSERT ... ON CONFLICT DO NOTHING` (see `evidence/adapters/base.py::bulk_insert_evidence_records`) after a real ~4.4M-row import proved the per-row pattern doesn't scale. The other three adapters still check-then-insert one row at a time, but at far smaller confirmed real-world scale (Panaya's curated sections cap at 20k rows each; HANA sizing/Readiness Check are in the hundreds) — not yet proven to need the same fix. Apply `bulk_insert_evidence_records` to them if/when a real file proves it necessary, rather than pre-emptively. | SPRINT-08 real-file validation (Signavio) | Low | Future (only if proven necessary) | Open |
| BL-012 | No automatic deduplication of re-imported evidence packages | Each upload of `POST /evidence-datasets` always creates a new independent `EvidenceDataset` row, even when `source_sha256` exactly matches an existing dataset, or when a different file of the same provider/type is uploaded again (e.g. a second Panaya export). This is partly intentional (ADR-017: "re-importing a new version must not silently overwrite historical provenance"), but an accidental true duplicate double-counts `records_count` and correlations with no warning. The dataset-delete endpoint (`DELETE /evidence-datasets/{id}`, SPRINT-08 post-completion) gives the user a manual way to remove an accidental duplicate; a same-sha256 warning at upload time would close the gap proactively. | User question during SPRINT-08 manual testing | Low | Future | Open |

## Rules
- Use a stable ID such as `BL-001`.
- Do not implement backlog items during the active sprint unless they are promoted into canonical sprint scope.
- A prerequisite defect that blocks the sprint is not backlog; it may be fixed as part of the active sprint and documented in progress.
- Architectural proposals must also respect ADR governance; a backlog entry cannot override an accepted ADR.
